# WorkGremlin 需求大纲 v0.1

- 版本：v0.1（leader 起草，2026-09-14）
- 状态：待 researcher 深化、待 main / 用户拍板
- 唯一工作目录：`/home/yinghui/work/WorkGremlin`（禁止在 `stb-insight*` 下写任何东西）
- 关联文档：`docs/tech-design.md`（coder，技术方案 v0.1）、`docs/test-strategy.md`（tester，测试策略 v0.1）、`docs/roadmap.md`（排期）

---

## 1. 产品定位

**一句话**：WorkGremlin 是一个本地桌面应用，把「多 agent 协作团队」的运行过程变成一块**看得见的工位**和一份**查得到的对话记录**。

当前用 CodeBuddy / 类 Claude-Code 的 multi-agent 团队跑任务时，用户面对的是一堆终端日志和一堆散落的 JSON 文件：谁在干活、谁卡住了、谁等谁的消息、刚才那句指令到底发出去没有，全都不可见。WorkGremlin 解决的就是这个**可观测性缺口**。

定位边界：

| 是 | 不是 |
| --- | --- |
| 本地单机的**只读监控 + 轻量管理**终端 | 云端 SaaS / 多人协作平台 |
| 团队运行过程的**仪表盘** | agent 的调度器 / 编排引擎（不替 runner 派活） |
| 对现有团队目录的**观测 + 上报接入** | 替换 CodeBuddy 团队机制本身 |
| 面向「正在带一支 agent 团队干活的人」 | 面向 agent 本身 |

---

## 2. 目标用户

| 画像 | 特征 | 核心诉求 | 关键场景 |
| --- | --- | --- | --- |
| **P1 主力：AI 团队操盘手**（开发者 / Tech Lead，正在用 multi-agent 跑一个真实项目） | 同时挂 3~8 个 agent；自己也在写代码；讨厌切窗口看日志 | 「一眼看清谁在干嘛、谁卡住了，卡在哪」 | 并行开发时盯盘；发现某 agent 阻塞 10 分钟立刻介入 |
| **P2：Agent 工程调优者**（做 prompt / 工作流调优） | 反复跑同一批任务对比效果；关心耗时分布和消息往返 | 「复盘一次完整协作：谁等了多久、消息往返多少次、产出在哪」 | 跑完一轮后回看时间线，定位协作瓶颈 |
| **P3：团队负责人 / 旁观者** | 不亲自下场，想知道钱和时间花在哪 | 「不看日志也能汇报进展」 | 开会时投屏工位视图；导出会话纪要 |

> 本阶段以 P1 为唯一设计靶心，P2/P3 的需求只在「不增加 P1 复杂度」的前提下顺带满足。

---

## 3. 核心功能清单

优先级约定：**P0 = 没有它产品不成立**（M0~M2 必须交付）；**P1 = 显著好用**（M3）；**P2 = 锦上添花 / 可延后**。

### 3.1 P0（必须）

#### P0-1 工位视图（Workstation View）
一块「办公室」区域，一个成员一张工位卡片，**自动实时刷新**（轮询 / 推送均可，但**不允许手动点按钮才更新**）。每张卡片至少展示：

| 字段 | 说明 | 数据来源 |
| --- | --- | --- |
| 成员名 / 角色 | leader / researcher / coder / tester … | roster（`config.json`）+ 上报 |
| **状态徽标** | 5 主状态 + degraded 标志（见 §4） | 上报为主，目录监听兜底 |
| **当前任务** | 任务标题 + 一句话描述 | 上报 |
| **进度百分比** | 0~100%，带进度条，**三层降级**（见下） | 上报 → 子任务推导 |
| **正在读写的文件** | 当前活跃文件路径（最多显示 3 条），**区分 `read` / `write` / `edit`**：读=弱化灰图标，写/改=高亮色条 | 上报（`file_activity.op`） |
| **最近产出** | 最近生成的产物（文件 / 提交 / 结论） | 上报 |
| **已耗时** | 当前任务已用时（秒级走动） | 由 `task.start` 时间戳推导，前端时钟走动 |
| 数据源标记 | 「上报值」/「推断值」/「degraded」图例 | 系统内部 |

**进度百分比三层降级（禁止编造）**：
1. 优先用 agent 上报的 `task.progress`（标记 `progress_source=reported`）；
2. 无上报但任务定义了子任务清单 → 用 `已完成子项 / 总子项` 推导，UI 标注「推导」（`progress_source=derived`）；
3. 两者都没有 → 显示**不确定进度条**（条纹动画，`progress_source=unknown`），**绝不显示 0% 或 100%**。

**容量与布局（Q3 已定）**：
- 成员上限 **16 人**（≥16 不串台是 P0 验收项）。
- **≥5 人**自动切**紧凑模式**：卡片缩小为「头像 + 状态点 + 单行任务」。
- **>16 人**时按 team 分页 / 折叠。
- 上述阈值可在设置里覆盖。

非功能要求：
- 状态变更后 **P95 ≤ 300ms** 反映到 UI（端到端，含传输）。
- 支持 **≥16 个成员**同屏不串台、不错位。
- 状态未知时**必须显示为未知/灰显，严禁编造或猜测数值**。

#### P0-2 对话记录窗口（Conversation View）
展示团队内**全部**消息流（不是只展示某一个成员的收件箱）。

- 每条消息展示：时间、发送方 → 接收方、消息类型、内容（长内容可展开看原文 JSON）。
- **过滤**：按成员（多选）、按时间范围、**按消息类型**。
- **搜索**：关键字全文搜索（M2 起用 SQLite FTS5，采用 **`tokenize='trigram'`** 分词器，零依赖且支持中文子串匹配；`unicode61` 会把整段中文当成一个 token，**不可用**）。**1~2 字的短查询自动降级为 `LIKE` 扫描**（限定时间范围控开销），因为 trigram 对 <3 字召回差。
- **滚动行为**：默认滚动到底自动跟随新消息；用户手动上滚时暂停跟随并显示「N 条新消息」提示，回到底部恢复跟随。
- **完整性**：消息**不丢、不重**（以 `dedupe_key` 保证幂等）、按时间有序。
- 容量：单 team **10 万条**消息下滚动流畅（虚拟滚动）。

#### P0-3 数据接入与落盘
- **B 路线（主，真值来源）**：agent 通过 `packages/reporter`（SDK / CLI）主动上报 → HTTP ingest → SQLite。
- **A 路线（兜底）**：`chokidar` 监听 `{workspace}/.codebuddy/teams/{team}/`，解析 `config.json`（roster）与各成员 mailbox（仅能拿到收件）。
- **降级规则**：无心跳 **60s** → 置 `degraded` 灰显并标注「推断」，**绝不编造状态**。
- 存储：SQLite（better-sqlite3）+ 内存快照；消息保留 **90 天 / 单 team 10 万条**，超限先导出 JSONL 归档再删除（归档逻辑 M1，M0 只预留 `archived_at` 与索引）。

#### P0-4 范围与多团队
- **单 workspace、多 team**：只观察当前工作区，但 `team` 是数据模型一等字段，UI 支持 team 切换。
- 多 workspace **不做**（架构预留，M0~M3 不实现）。

#### P0-5 连接与自解释
- 顶部连接状态条：WS 连接状态、数据源（上报 / 推断）、最近同步时间。
- 断线自动重连（指数退避），失焦/休眠恢复后自动补齐。

#### P0-6 离线演示模式（`--demo` / `MOCK=1`）
M0 就要有：内置确定性假数据源，既能给用户演示，也是 E2E 的确定性数据源。

#### P0-7 消息脱敏与凭据防护（安全底线）
- agent 对话常粘贴 `.env` / 密钥，**写入 SQLite 前必须正则脱敏替换为 `***`**，原文**不入库**（不是仅 UI 隐藏）。
- 规则至少覆盖：`sk-[A-Za-z0-9]{16,}`、`AKIA[0-9A-Z]{16}`、`ghp_[A-Za-z0-9]{36}`、JWT（`eyJ...`）、`-----BEGIN *PRIVATE KEY-----`、`(?i)(password|passwd|pwd|token|secret|api[_-]?key|authorization)\s*[:=]\s*\S+`、URL 内嵌凭据 `://user:pass@`。
- UI 对命中规则的消息显示「本条已脱敏」标记。
- M0 只预留 `redaction` 钩子（一个可替换的 `redact(content)` 中间件点），M1 随消息入库一并启用。
- **真正的底线是「脱敏在入库前完成」** —— 即 DB 里**从一开始就不存在明文**。若在 DB 中发现明文，那是 **P0 级缺陷**，不是靠任何开关能补救的。脱敏必须发生在**入库与 FTS 建索引之前**。
- **纵深防御（不要当作安全底座）**：`secure_delete` 只是额外一层，防的是「删除已脱敏消息时旧页残留」。
  - **`secure_delete` 是连接级 PRAGMA，不是表级**，故采用**双连接**：**写入连接** `secure_delete=OFF`（高吞吐，承担 1000 事件/s 稳态）；**删除/归档连接** `secure_delete=ON`（仅用于归档删除与手动删除）。正常流程几乎不删，稳态吞吐不受影响。
  - WAL：显式设置 `wal_autocheckpoint`；归档/删除后在删除连接执行 `PRAGMA wal_checkpoint(TRUNCATE)`；`.db` / `.db-wal` / `.db-shm` 文件权限 `0600`。
  - **禁止为吞吐自行关闭 `secure_delete`**；若实测仍冲突，走「归档批量化 + 降频（同步改异步批处理）」，不削弱安全项。

**看原文的加密通道（Q9 已定，归 M3 / P1，M0/M1 不实现）**
- **默认关闭**（不存原文）。开启需用户在设置里**显式确认并理解风险**。
- 开启时：优先用 **OS 钥匙串**保管随机数据密钥（macOS Keychain / Windows DPAPI / Linux libsecret，统一用 `keytar` 封装）；**口令派生（scrypt / argon2）作为无钥匙串环境的降级方案**。
- 约束：原文与正文**分表存储**、独立加密；**每次会话重新授权，不记住解锁状态**；查看原文**记入事件日志**。

---

### 3.2 P1（M3 目标）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| P1-1 | 桌面打包分发 | electron-builder 产出 AppImage / deb / dmg（**仅 x64**：Linux / Windows / macOS-x64；不出 ARM，但脚本参数化不硬编码架构） |
| P1-2 | 团队时间线与吞吐图 | ECharts：一天的任务分布、成员吞吐、消息量 |
| P1-3 | 会话导出 | 导出会话为 Markdown / JSONL，便于汇报与复盘 |
| P1-4 | 消息归档与保留策略落地 | 90 天 / 10 万条超限自动归档 |
| P1-5 | 暗色主题 | 跟随系统 |
| P1-6 | 只读+轻量动作 | 标记已读、导出、复制消息原文（**不下发指令**） |
| P1-7 | **告警与通知（Q4 已定）** | **仅 S1 级触发**：阻塞、出错、离线 >60s。系统通知**默认开**、声音**默认关**；支持免打扰时段与按成员静音。M1 先做站内提示（连接条 / 卡片红点），系统通知与免打扰 M3 |

### 3.3 P2（延后 / 待评估）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| P2-1 | ~~反向控制~~ → **已升为 M4（范围收窄）** | M4 只做最保守两层：①向指定成员投递任务消息（派活）②中断当前任务（发中断信号）。**不做**代码级干预、不改 prompt、不直接操作用户文件。所有反向操作必须 UI 二次确认 + 记入事件日志。M0~M3 不实现，架构预留指令通道 |
| P2-2 | 多 workspace 同时观察 | 架构预留 |
| P2-3 | 崩溃回放 / 事件时间旅行 | 依赖 `events` 表全量审计 |
| P2-4 | Tauri 迁移评估（包体瘦身） | Electron ~100MB **已最终确认接受**，M3 后再评估 |
| P2-5 | 网络盘 / WSL 场景支持 | M0~M2 明确不支持；保留 `EMFILE` / 网络盘降级 5s 轮询并置 degraded；WSL 与时序跳变登记 M3 评估 |

---

## 4. 状态语义（v0.1 基线，researcher 负责深化）

> 已由 main 拍板：**5 个主状态 + 1 个正交标志**，不是 6 个平级状态。

| 状态 | 含义（v0.1） | 判定来源 |
| --- | --- | --- |
| `offline` | 成员进程不存在 / 从未上报且不在 roster 活跃列表 | roster + 心跳缺失 |
| `idle` | 在线但无任务在执行（在等派活） | 上报 `status=idle` 或 task 结束且未下线 |
| `busy` | 正在执行任务（有活跃 task） | 上报 `task.start` / `task.progress` |
| `blocked` | 任务无法推进，在等外部依赖（等人回复、等命令、等资源） | 上报 `status=blocked` + 阻塞原因 |
| `error` | 执行失败 / 进程异常退出 | 上报 `status=error` 或任务以失败结束 |

**正交标志 `degraded`**：数据可信度下降（超过 60s 无心跳、或值来自 A 路线推断），UI 灰显 + 标注「推断」。`degraded` 可与任一主状态组合（如 `busy + degraded`）。

**已废弃**：`unknown` 不再作为第 6 个平级状态——无法判定时归入 `offline` 或打 `degraded` 标志，由 researcher 在《状态语义定义》中明确优先序。

---

## 5. 里程碑（摘要，详见 `docs/roadmap.md`）

| 里程碑 | 目标 | 关键交付 |
| --- | --- | --- |
| **M0** | 骨架 + 可测性地基 + demo | monorepo 骨架、Electron 空壳、两个界面 mock 静态渲染、better-sqlite3 + electron-rebuild 跑通、`--demo` 模式、reporter CLI 示例、5 条可测性契约 |
| **M1** | 真实状态接入 | A 路线 watcher/parser + B 路线 ingest + reporter SDK、SQLite 落库、snapshot 推送、工位视图真实数据上线 |
| **M2** | 对话记录 + 持久化 | 消息全量入库与增量推送、过滤/搜索（FTS5）、虚拟滚动、归档保留策略、崩溃恢复 |
| **M3** | 打包分发与增强 | electron-builder（x64 三平台）、时间线图、导出、暗色主题 |
| **M4** | 反向控制（保守版）+ 监听生产化 | 派活消息投递、中断当前任务（UI 二次确认 + 事件日志）；目录监听生产化 |

---

## 6. 开放问题 Q1~Q7 —— 已拍板，结论如下

| # | 问题 | **拍板结论** | 落地位置 |
| --- | --- | --- | --- |
| Q1 | 文件读写是否区分动作？ | **区分**。`file_activity.op` 取 `read` / `write` / `edit` 三值；UI：读=弱化灰图标，写/改=高亮色条 | P0-1 工位视图；coder M0 建表预留 `op` 字段 |
| Q2 | 进度百分比来源？ | **三层降级，禁止编造**：①上报 `task.progress`②无上报时用 `已完成子项/总子项` 推导并标「推导」③都没有 → **不确定进度条**（条纹动画），**绝不显示 0% 或 100%** | P0-1；coder M0 建表预留 `task.progress` + `progress_source` |
| Q3 | 成员上限与紧凑模式？ | 上限 **16 人**（≥16 不串台是 P0 验收项）；**≥5 人**自动切紧凑模式（头像+状态点+单行任务）；**>16** 按 team 分页/折叠；阈值可在设置覆盖 | P0-1 布局规则；tester 增加相关用例 |
| Q4 | 要不要声音/系统通知？ | **要做，严格分级**：仅 **S1 级**（阻塞、出错、离线 >60s）触发；系统通知**默认开**、声音**默认关**；支持免打扰时段与按成员静音 | P1-7；M1 先做站内提示，系统通知与免打扰 M3 |
| Q5 | 消息是否脱敏？ | **必须做**。入库**前**正则脱敏为 `***`，**原文不入库**；7 类规则（见 P0-7）；UI 标「本条已脱敏」；看原文走显式二次确认 + 单独加密通道（P1） | **升为 P0-7**；coder M0 预留 `redaction` 钩子，M1 启用 |
| Q6 | Electron ~100MB 包体？ | **最终确认接受**，不再讨论 | — |
| Q7 | 反向控制做不做？ | **做，放 M4，只做最保守一层**：①向指定成员投递任务消息（派活）②中断当前任务。**不做**代码级干预、不改 prompt、不直接操作用户文件。所有反向操作须 UI 二次确认 + 记入事件日志 | P2-1 已改写；M0~M3 不实现，架构预留指令通道 |

### 6.1 第二轮开放问题 Q8~Q11 —— 已拍板

| # | 问题 | **拍板结论** | 落地位置 |
| --- | --- | --- | --- |
| Q8 | 免打扰与静音规则？ | 免打扰**默认关闭**、不预设时段（避免新用户误以为「没通知」），设置里提供 `22:00–08:00` **模板一键启用**。静音/聚合规则：①仅 S1 触发；②**同一成员 + 同一故障类型，5 分钟窗口内聚合为 1 条**；③成员离线期间不重复提醒，恢复上线时合并为「离线期间发生 N 次」；④**窗口失焦才弹系统通知，聚焦时只走站内** | P1-7；M1 站内提示，M3 系统通知 |
| Q9 | 原文加密通道形态？ | **默认关闭**（不存原文）。开启需显式确认风险；优先 **OS 钥匙串**（`keytar` 封装 Keychain / DPAPI / libsecret）保管随机数据密钥，**口令派生（scrypt/argon2）为无钥匙串环境的降级**；原文与正文**分表 + 独立加密**；**每次会话重新授权、不记住解锁状态**；查看原文记事件日志 | P0-7 附章；**M3**，M0/M1 不实现 |
| Q10 | 中文 FTS5 分词？ | 采用 **FTS5 内置 `trigram` tokenizer**（零依赖、支持中文子串）；`unicode61` 会把整段中文当一个 token，**禁用**。**1~2 字查询自动降级为 `LIKE` 扫描**（限定时间范围控开销）。M2 实测召回不足再评估 jieba 预分词 | P0-2 搜索；**coder M0 建表即用 trigram**，避免后期重建索引迁移 |
| Q11 | 无 macOS 开发者账号？ | **书面豁免接受**。降级：ad-hoc 签名 + 发布页/README/release notes/应用内「关于」页均明示「未公证，首次启动需右键 → 打开 / 系统设置 → 隐私与安全性 → 仍要打开」。**不阻塞任何里程碑** | M3 打包 |

---

## 7. 已拍板决策（本版已并入，不再讨论）

1. 数据源：**A + B 混合**，B（主动上报）为真值，A（目录监听）兜底；无心跳 60s 置 `degraded`，禁止编造状态。
2. 桌面外壳：**Electron 38 + Vue 3 + Vite + TypeScript**。
3. 数据库：**better-sqlite3 + @electron/rebuild**（不用 `node:sqlite`）；Electron 版本锁定并接 `postinstall`。
4. 范围：**单 workspace、多 team**；多 workspace 不做。
5. M0：**一次性交付全骨架**（约 30 个文件）。
6. 状态模型：**5 主状态 + degraded 正交标志**。
7. 消息保留：**90 天 / 单 team 10 万条**，超限先导出 JSONL 归档再删除（归档逻辑 M1，M0 预留字段与索引）。
8. 平台：**仅 x64**（Linux/Windows/macOS-x64），不出 ARM，打包脚本参数化。
9. 网络盘 / WSL：M0~M2 不支持；保留 `EMFILE` / 网络盘降级 5s 轮询并置 `degraded`；WSL 与时序跳变登记 M3 评估。
10. 离线演示模式 `--demo` / `MOCK=1`：**提前到 M0**，与 mock 静态渲染、E2E 确定性数据源三合一。
11. tester 的 15 条可测性契约中 **5 条**（时间源可注入、`createTestApp` 工厂、core 与 Electron 解耦、`data-testid`、确定性 ID）由 coder 在 M0 落地。
12. **存储层安全基线**：**底线是脱敏在入库前完成**（DB 中从不存在明文，出现明文即 P0 缺陷）；`secure_delete` 仅为纵深防御，因其是**连接级** PRAGMA 而采用**双连接**（写入连接 OFF 保吞吐 / 删除归档连接 ON 防残留）+ WAL checkpoint(TRUNCATE) + 文件权限 0600。**两个指标都不放宽**。
13. **FTS5 分词器固定为 `trigram`**，M0 建表即用；短查询（1~2 字）降级 `LIKE`。
14. **通知聚合规则**：仅 S1 触发；同成员+同故障类型 5 分钟聚合 1 条；离线期间不重复提醒、上线合并为「离线期间发生 N 次」；失焦才弹系统通知。
15. **macOS 公证豁免**：ad-hoc 签名 + 四处明示未公证说明；不阻塞任何里程碑。
